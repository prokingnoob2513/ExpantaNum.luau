# ExpantaNum.luau
[ExpantaNum.js](https://github.com/Naruyoko/ExpantaNum.js) ported to Roblox (10{{1}}1.79e308 limit)

## How to load
1. Create a module
<sup>1.1. Name it Stand Proud Ron /j</sup>
2. Copy the [code](https://github.com/prokingnoob2513/ExpantaNum.luau/blob/main/ExpantaNum.luau)
or [get the model](https://create.roblox.com/store/asset/114410057383638/ExpantaNum)
3. Add this to one of your scripts:
```
local ExNum = require(path.to.ExpantaNum)
```

You can use metamethods for ExpantaNum
Functions: correct, fromNumber, toNumber, toDisplay, toString, fromString, toExpanta, eq, le, me, meeq, leeq, abs, neg, cmp, max, min, log10, isint, recip, pow, mod, root, mul, floor, ceil, div, add, sub, sqrt, log, exp, maxabs, pow10, gamma, fact, rand, exporand, toBigNum, toScientific, toShortScientific, short, toEnt, toShortEnt, toEs, toShortEs, toHyperE (only works IF num < 10{9007199254740992}10), toShortHyperE (only works IF num < 10{9007199254740992}10), lambertw, slog, tetrate, pentate, arrow (only works IF x = 10), lbencode (soon), lbdecode (soon)

number library, big number, big num, bignumber, bignum, big integer, biginteger, bigint, incremental games, idle games, large numbers, huge numbers, googology, roblox, luau
